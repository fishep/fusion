package com.fishep.fusion.gateway.feign.impl;

//import com.fishep.fusion.common.response.Result;
import com.fishep.fusion.gateway.feign.UserFeign;
import com.fishep.fusion.gateway.feign.response.UserResponse;
import org.springframework.stereotype.Component;

//@Component
public class UserFeignImpl implements UserFeign {

//    @Override
//    public Result<UserResponse> one(Long id) {
//        return new Result<>(400, "服务不可达,服务降级,UserFeignImpl one(Long id), " + id);
//    }
}
